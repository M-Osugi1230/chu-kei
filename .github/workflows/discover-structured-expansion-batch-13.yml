name: Discover Structured Expansion Batch 13

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-fallback:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use scripts/rank_official_ir_candidates_v2.py for the normal lightweight evidence-ranking flow."
