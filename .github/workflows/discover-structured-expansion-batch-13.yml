name: Discover Structured Expansion Batch 13 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by scripts/rank_official_ir_candidates_v2.py"
